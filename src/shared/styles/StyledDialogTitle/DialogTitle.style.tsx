// components
import { Box } from '@mui/material';

// utils
import { styled } from '@mui/material';

export const StyledDialogTitle = styled(Box)(({ theme }) => ({
	padding: '1.12vw 1.12vw 0.69vw 1.12vw',
}));
